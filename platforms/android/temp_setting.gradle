android {
            defaultConfig {
                applicationId = "org.nativescript.blankng"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }